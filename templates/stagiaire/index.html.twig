{% extends 'base.html.twig' %}

{% block title %}Hello StagiaireController!{% endblock %}

{% block body %}
    <h1>Stagiaire index</h1>

    <table class="table">
        <thead>
            <tr>
                
                <th>Nom</th>
                <th>Prénom</th>
                <th>dateNaissance</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for stagiaire in stagiaires %}
            <tr>
                
                <td>{{ stagiaire.nom }}</td>
                <td>{{ stagiaire.prenom }}</td>
                <td>{{ stagiaire.dateNaissance ? stagiaire.dateNaissance|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('stagiaire_show', {'id': stagiaire.id}) }}">show</a>
                    <a href="{{ path('stagiaire_edit', {'id': stagiaire.id}) }}">edit</a>
                    <a href="{{ path('stagiaire_pdf', {'id': stagiaire.id}) }}">PDF</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('stagiaire_new') }}">Create new</a>
{% endblock %}
