{% extends 'base.html.twig' %}


{% block body %}
    


    <div class="jumbotron">
  <h1 class="display-4">{{ salle.nomSalle }}</h1>
  <p class="lead">{{ salle.nbplaces }}</p>
  <hr class="my-4">
  <p>Materiels dans la salle: {% for materiel in salle.posseder %}
                    {{ materiel }}
                {% endfor %}</p>
  
  <a class="btn btn-warning btn-lg" href="{{ path('add_posseder', {'id': salle.id}) }}" role="button">Modifier matériels </a>
  <a class="btn btn-primary btn-lg" href="{{ path('salle_index') }}" role="button">Retour liste des salles</a>
</div>


    {{ include('salle/delete_form.html.twig') }}

    
{% endblock %}
