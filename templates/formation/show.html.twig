{% extends 'base.html.twig' %}


{% block body %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Formation</th>
                <td>{{ formation.nomFormation }}</td>
            </tr>

            <tr>
                <th>Descriptif</th>
                <td>{{ formation.descriptif }}</td>
            </tr>
            <tr>
                <th>Ateliers</th>
                <td>
                {% for atelier in formation.duree %}
                    {{ atelier }}
                {% endfor %}
                </td>
                
            </tr>
        </tbody>
    </table>

     <a href="{{ path('duree_index', {'id': formation.id}) }}">Ajouter atelier</a>

     <a href="{{ path('duree_index', {'id': formation.id}) }}">Modifier atelier</a>

     

    <a href="{{ path('formation_index') }}">Retour liste formations</a>
{% endblock %}
